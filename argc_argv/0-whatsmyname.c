#include <stdio.h>

/**
 * j'utilise argc et argv
 *la condition compare argc 
 *printf pour afficher le nom
*/

int main(int argc, char *argv[])
{
        if(argc == 1)
        printf("%s\n", argv[])
        return(0)
}
