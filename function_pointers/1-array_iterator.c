void array_iterator(int *array, size_t size, void (*action)(int))
{
   int *array[0], *size(0);
   for (array = 0; array < count; array++)
   {
        if (action == NULL && size == NULL)
   }
        action(array);
    
   
     
}